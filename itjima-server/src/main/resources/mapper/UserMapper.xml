<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.itjima_server.mapper.UserMapper">
  <select id="findByEmail" resultType="boolean">
    SELECT COUNT(*) > 0
    FROM USERS
    WHERE email = #{email}
  </select>
  <select id="findByPhone" resultType="boolean">
    SELECT COUNT(*) > 0
    FROM USERS
    WHERE phone = #{phone}
  </select>
  <insert parameterType="com.itjima_server.domain.User" id="insert" useGeneratedKeys="true"
    keyProperty="id">
    INSERT INTO USERS (name, email, password, phone, provider)
    VALUES (#{name}, #{email}, #{password}, #{phone}, #{provider})
  </insert>
</mapper>