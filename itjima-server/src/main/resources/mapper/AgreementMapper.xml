<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.itjima_server.mapper.AgreementMapper">
  <insert id="insert" parameterType="com.itjima_server.domain.Agreement" useGeneratedKeys="true"
    keyProperty="id">
    INSERT INTO AGREEMENTS (item_id, status, amount, due_at, terms)
    VALUES (#{itemId}, #{status}, #{amount}, #{dueAt}, #{terms})
  </insert>
  <select id="findById" resultType="com.itjima_server.domain.Agreement">
    SELECT id,
           item_id,
           status,
           amount,
           due_at,
           terms,
           created_at,
           updated_at
    FROM AGREEMENTS
    WHERE id = #{id}
  </select>
  <update id="updateStatusById">
    UPDATE AGREEMENTS
    SET status = #{status}
    WHERE id = #{id}
  </update>
</mapper>